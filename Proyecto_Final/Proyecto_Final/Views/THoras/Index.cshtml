@model IEnumerable<Proyecto_Final.Models.THora>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Hora)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Hora)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HoraId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HoraId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HoraId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
